description: The caller exceeded an enforced rate limit.
headers:
  Retry-After:
    $ref: ../headers/retry-after.yaml
  X-Request-ID:
    $ref: ../headers/request-id.yaml
content:
  application/problem+json:
    example:
      type: https://moco.araihu.com/problems/too-many-requests
      title: Too Many Requests
      status: 429
      code: rate_limited
      requestId: 11111111-1111-4111-8111-111111111111
    schema:
      $ref: ../schemas/problem.yaml
