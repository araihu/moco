description: The secret representation has not changed since the supplied ETag.
headers:
  Cache-Control:
    $ref: ../headers/cache-control.yaml
  ETag:
    $ref: ../headers/etag.yaml
  X-Request-ID:
    $ref: ../headers/request-id.yaml
