name: prefix
in: query
required: false
description: |
  Slash-delimited prefix. One trailing slash is allowed; empty, `.`, and `..`
  segments are invalid. Omit it to list the entire vault.
schema:
  type: string
  minLength: 1
  maxLength: 1024
  pattern: '^[^/]+(?:/[^/]+)*/?$'
