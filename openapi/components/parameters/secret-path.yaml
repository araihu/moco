name: path
in: query
required: true
description: |
  Slash-delimited logical secret path. Empty, `.`, and `..` segments are
  invalid. Paths can be sensitive metadata and should not be logged by clients.
schema:
  type: string
  minLength: 1
  maxLength: 1024
  pattern: '^[^/]+(?:/[^/]+)*$'
